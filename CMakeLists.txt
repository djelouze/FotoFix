PROJECT( FotoFix )

CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

FIND_PACKAGE( ITK REQUIRED )
INCLUDE( ${ITK_USE_FILE} )

INCLUDE_DIRECTORIES( Filters )

ADD_EXECUTABLE( fotofix fotofix.cxx )
TARGET_LINK_LIBRARIES( fotofix ${ITK_LIBRARIES} )
